第一周
先把 product config + sync + versions + sqlite 打通。
不要急着接 LLM。先证明“多产品版本发现”是稳定的。

第二周
做 fetch + normalize + raw/normalized snapshot。
这一步一旦稳了，后面所有分析都站得住。

第三周
接 LLM，先只做单版本分析，不做 digest。
把输出模板压实。

第四周
做 digest weekly 和缓存体系。
这样产品就已经有“可连续使用”的味道了。