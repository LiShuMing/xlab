#include "../include/fwd.h"

class Solution {
public:
    bool pyramidTransition(string bottom, vector<string>& allowed) {
        return false;
    }
};

int main() {
    Solution solution;
    cout << solution.pyramidTransition("BCD", {"BCG", "CDE", "GEA", "FFF"}) << endl;
    return 0;
}